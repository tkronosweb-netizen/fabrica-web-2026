<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= data.brandName %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
        .hero { height: 80vh; background: linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.6)), url('<%= data.heroImg %>'); background-size: cover; background-position: center; display: flex; align-items: center; justify-content: center; color: white; text-align: center; }
        .section-title { font-weight: 800; margin-bottom: 50px; text-align: center; }
        .glass-card { background: #f8fafc; border-radius: 20px; padding: 30px; border: none; transition: 0.3s; height: 100%; }
        .glass-card:hover { transform: translateY(-10px); box-shadow: 0 15px 30px rgba(0,0,0,0.05); }
    </style>
</head>
<body>
    <header class="hero">
        <div class="container">
            <% if(data.logo) { %><img src="<%= data.logo %>" height="100" class="mb-4"><% } %>
            <h1 class="display-1 fw-bold"><%= data.brandName %></h1>
        </div>
    </header>

    <div class="container py-5">
        <section class="py-5">
            <h2 class="section-title">Nuestra Historia</h2>
            <p class="fs-5 text-center text-muted" style="max-width: 800px; margin: 0 auto;"><%= data.aboutText %></p>
        </section>

        <section class="py-5">
            <h2 class="section-title">Nuestros Productos</h2>
            <div class="row g-4">
                <% (data.products || []).forEach(p => { %>
                    <div class="col-md-4"><div class="glass-card"><h4><%= p.name %></h4><p><%= p.desc %></p><div class="fs-4 fw-bold text-primary"><%= p.price %></div></div></div>
                <% }) %>
            </div>
        </section>

        <section class="py-5 bg-dark text-white rounded-5 p-5 mt-5">
            <div class="row align-items-center">
                <div class="col-md-6">
                    <h2 class="fw-bold mb-4">Contactanos</h2>
                    <p class="fs-5 mb-1">📍 <%= data.address %></p>
                    <p class="fs-5 mb-1">📧 <%= data.email %></p>
                    <p class="fs-5">💬 WhatsApp: <%= data.whatsapp %></p>
                </div>
                <div class="col-md-6">
                    <a href="https://wa.me/<%= data.whatsapp %>" class="btn btn-primary btn-lg w-100 py-3 rounded-pill fw-bold">Hablemos por WhatsApp</a>
                </div>
            </div>
        </section>
    </div>
</body>
</html>
