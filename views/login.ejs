<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ingresar - Fábrica Elite</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background: linear-gradient(135deg, #4f46e5 0%, #3b82f6 100%); height: 100vh; display: flex; align-items: center; font-family: 'Inter', sans-serif; }
        .auth-card { background: white; border-radius: 24px; padding: 40px; box-shadow: 0 20px 40px rgba(0,0,0,0.1); width: 100%; max-width: 400px; }
        .btn-auth { background: #2563eb; color: white; border-radius: 12px; padding: 12px; font-weight: 600; border: none; width: 100%; transition: 0.3s; }
        .btn-auth:hover { opacity: 0.9; }
    </style>
</head>
<body>
    <div class="container d-flex justify-content-center">
        <div class="auth-card">
            <h2 class="fw-bold mb-2 text-center">¡Hola de nuevo!</h2>
            <p class="text-muted text-center mb-4">Ingresá a tu panel de control.</p>
            <form action="/login" method="POST">
                <div class="mb-3">
                    <label class="form-label small fw-bold">Email</label>
                    <input type="email" name="email" class="form-control p-3" required>
                </div>
                <div class="mb-4">
                    <label class="form-label small fw-bold">Contraseña</label>
                    <input type="password" name="password" class="form-control p-3" required>
                </div>
                <button type="submit" class="btn-auth mb-3">Ingresar</button>
                <p class="text-center small text-muted">¿No tenés cuenta? <a href="/registro" class="text-primary text-decoration-none fw-bold">Registrate acá</a></p>
            </form>
        </div>
    </div>
</body>
</html>
